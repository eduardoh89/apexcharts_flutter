include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - reference/**
    - example/**
  language:
    strict-casts: true
    strict-raw-types: true

linter:
  rules:
    prefer_relative_imports: true
    avoid_print: true
